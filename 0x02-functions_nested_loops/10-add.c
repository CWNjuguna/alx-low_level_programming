#include "main.h"

/**
 * add - adds two integers and returns the result
 * @w: int to be added to n
 * @n: int to be added to w
 * Return: sum of w and n
 */
int add(int w, int n)
{
	int c;

	c = w + n;
	return (c);
}
